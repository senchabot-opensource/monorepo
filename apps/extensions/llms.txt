# Senchabot Extensions

Free, open-source customizable stream overlays, multi-chat widgets, subscriber goal plants, floating emote wall overlays, transparent chat boxes, and interactive streaming tools for Twitch and Kick. Configure and embed overlays and tools into OBS Studio, Streamlabs Desktop, XSplit Broadcaster, vMix, Lightstream, PRISM Live Studio, Twitch Studio, Meld Studio, Wirecast, Ecamm Live, or any software that supports browser sources in seconds. No account or login required.

## What It Does

Senchabot Extensions provides 100% free customizable stream overlays, multi-chat widgets, subscriber goal plants, floating emote wall overlays, transparent chat box overlays, and interactive streaming tools for Twitch and Kick streamers. Each overlay and tool runs seamlessly as a browser source that you can add to OBS Studio, Streamlabs Desktop, XSplit Broadcaster, vMix, Lightstream, PRISM Live Studio, or any streaming software supporting browser sources in seconds.

- **Sub Sprout** — A 100% free visual subscriber goal plant overlay (no login required) that grows with each subscription, resub, or gift sub on Twitch and Kick. Resets after reaching full growth.
- **Universal Chat** — A 100% free multi-chat widget and stream chat box overlay (no login required) that combines Twitch and Kick chat into a single on-screen feed with 7TV emotes, badges, and customizable stream overlay styles.
- **Raffle** — A 100% free chat-based giveaway tool and raffle picker (no login required) with custom keyword entry and a live confetti winner celebration overlay for OBS Studio, Streamlabs Desktop, XSplit, and more.
- **OBS Bridge** — A 100% free chat-controlled OBS scene switcher and recording broadcast tool (no login required) with custom command trigger naming via local OBS WebSocket.
- **Emote Wall** — A 100% free floating emote overlay (no login required) that turns emote-only Twitch, Kick, and 7TV chat messages into floating on-screen emotes with Calm, Chaos or Bounce animations.

## Tech Stack

- React 19 + TypeScript
- Vite 7
- TanStack Router (file-based routing)
- TanStack Start
- Tailwind CSS v4
- Cloudflare Workers (deployment)
- Vitest (testing)
- Biome (linting/formatting)

## Project Structure

```
.
├── src/routes/
│   ├── index.tsx                   # Widget and tool hub landing page
│   ├── __root.tsx                  # Root layout with SEO meta tags
│   ├── setup/
│   │   ├── sub-growing-plant.tsx   # Sub Sprout configuration page
│   │   ├── chat-widget.tsx         # Universal Chat configuration page
│   │   ├── raffle.tsx              # Raffle configuration page
│   │   ├── emote-wall.tsx          # Emote Wall configuration page
│   │   └── obs-bridge.tsx          # OBS Bridge configuration page
│   ├── tools/
│   │   └── obs-bridge.tsx          # OBS Bridge live WebSocket receiver frame
│   └── widgets/
│       ├── sub-sprout-widget.tsx   # Sub Sprout overlay (Browser Source)
│       ├── chat-widget.tsx         # Universal Chat overlay (Browser Source)
│       ├── emote-wall.tsx          # Emote Wall overlay (Browser Source)
│       └── raffle-overlay.tsx      # Raffle winner overlay (Browser Source)
├── src/features/
│   ├── tools/
│   │   └── use-chat.ts             # OBS Bridge chat command & WebSocket coordinator
│   └── widgets/
│       ├── sub-sprout/
│       │   ├── sub-sprout-widget.tsx
│       │   ├── plants/registry.ts  # Plant definitions (classic, rose, sunflower, etc.)
│       │   └── water/watering-fx.ts # Watering animations
│       ├── chat-widget/
│       │   ├── use-unified-chat.ts # Chat connection hook (Twitch IRC + Kick Pusher)
│       │   ├── chat-messages.ts    # Message parsing & 7TV emote tokenizer
│       │   ├── use-badges.ts       # Twitch & Kick sub badge loader
│       │   └── kick-badges.tsx     # Kick badge SVGs
│       ├── raffle/
│       │   ├── raffle-widget.tsx   # Raffle setup UI & state manager
│       │   └── raffle-overlay.tsx  # Winner celebration overlay (canvas-confetti)
│       └── emote-wall/
│           ├── emote-wall.tsx      # Floating emote overlay (Calm/Chaos/Bounce)
│           ├── emote-pops.ts       # Calm/Chaos/Bounce spawn & flight factories
│           └── emote-utils.ts      # Emote-only message detection (Twitch/Kick/7TV)
├── src/hooks/
│   ├── use-raffle-state.ts         # localStorage-based raffle state
│   └── use-raffle-chat.ts          # Chat monitoring for raffle entries
├── src/types/
│   └── raffle.ts                   # Raffle type definitions
├── src/lib/
│   ├── kick.ts                     # Kick Pusher & API utilities
│   └── twitch.ts                   # Twitch IRC client (anonymous justinfan)
├── src/components/
│   ├── breadcrumb.tsx              # Navigation breadcrumb
│   └── youtube-tutorial.tsx        # Video tutorial embedder
├── src/styles.css                  # Tailwind CSS entry
├── src/router.tsx                  # TanStack Router setup
├── vite.config.ts                  # Vite + TanStack Router config
├── package.json
└── README.md
```

## Detailed Widget & Tool Specifications

### 1. Sub Sprout — Subscriber Goal Plant Overlay (`/setup/sub-growing-plant`)

A customizable subscriber goal plant overlay that levels up and blossoms in real time as subscribers join on Twitch or Kick. The widget itself lives at `/widgets/sub-sprout-widget` and can be used directly with URL query parameters — no setup UI required.

- **Event Listening**: Connects anonymously to Twitch IRC for subs, resubs, and gift subs (via tmi.js) and Kick Pusher WebSocket for subscription events. Both platforms can be connected simultaneously in a single browser source.
- **Multi-Platform Support**: Pass `twitch=<channel>` and/or `kick=<channel>` to listen to both platforms at once.
- **Plant Varieties (`variety`)**: `classic`, `rose`, `sunflower`, `cactus`, `tulip`, `pine`, `lotus`, `lily`, `palm`, `vine` (default: `classic`).
- **Plant Changing Modes (`pick`)**: `fixed` (default, restarts the same plant after full growth), `cycle` (loops through plant varieties in order), `random` (picks a random variety after each cycle).
- **Watering Effects (`water`)**: `off` (default), `rain`, `sparkle`.
- **Sub Count Badge (`countfx`)**: `1` (default) shows bundle quantities (e.g. `x5`); `0` disables it.
- **Pot Stage Label (`potlabel`)**: `1` draws the current stage over total stages (e.g. `3/10`) as text on the flower pot; omitted (default) hides it.
- **Preview Simulation (`simulate`)**: `auto` simulates x1/x2 subs until a real connection is established (used by the setup preview); `1`/`true` forces always-on simulation; `0`/`off` (default) means real events only.
- **Moderator & Broadcaster Testing**: Broadcasters and channel moderators can type `!grow` in chat to manually trigger plant growth.
- **Legacy Params**: Older URLs using `channel` + `platform` still work. `channel` maps to the Twitch channel when `platform=twitch` (default) and to the Kick channel when `platform=kick`.
- **Direct URL examples**:
  - Twitch only: `https://extensions.senchabot.com/widgets/sub-sprout-widget?twitch=senchabot`
  - Kick only: `https://extensions.senchabot.com/widgets/sub-sprout-widget?kick=senchabot`
  - Both platforms: `https://extensions.senchabot.com/widgets/sub-sprout-widget?twitch=senchabot&kick=senchabot`
  - Customized: `https://extensions.senchabot.com/widgets/sub-sprout-widget?twitch=senchabot&variety=rose&pick=cycle&water=rain&countfx=0`
- **URL Query Parameters**:
  - `twitch` (string, Twitch channel name)
  - `kick` (string, Kick channel name)
  - `variety` (plant id string, default `classic`)
  - `pick` (`fixed` | `cycle` | `random`, default `fixed`)
  - `water` (`off` | `rain` | `sparkle`, default `off`)
  - `countfx` (`0` | `1`, default `1`)
  - `potlabel` (`0` | `1`, default `0`)
  - `simulate` (`auto` | `1`/`true` | `0`/`off`, default off)
  - Legacy: `channel` (string), `platform` (`twitch` | `kick`, default `twitch`)

---

### 2. Multiplatform Stream Chat Box (`/setup/chat-widget`)

A customizable multi-chat widget and stream chat box overlay that merges Twitch and Kick chat into a single on-stream feed.

- **Cross-Platform Dual Stream**: Simultaneously connects to Twitch IRC anonymously (`justinfan`) and Kick Pusher WebSocket without account login.
- **7TV Emote Integration**: Fetches and renders static and animated 7TV global and channel emotes automatically with zero streamer extensions required.
- **Badge Rendering**: Parses and renders Twitch and Kick subscriber, moderator, VIP, founder, and broadcaster badges.
- **Message Layout Styles (`layout`)**:
  - `inline`: Standard stream chat (Username: message).
  - `stacked`: Username header displayed above message body.
  - `card`: Message displayed in modern bubble/card format.
  - `compact`: Dense, Twitch-native spacing for high-velocity chats.
- **Entry Animations (`animation`)**: `slide` (from right), `pop` (scale-in), `bounce`, `stagger`, `fade`, and `none`.
- **Typography (`font`)**: Selectable fonts including `inter`, `roboto`, `nunito`, `mono` (JetBrains Mono), `serif` (Source Serif 4), and `system`.
- **Styling Options**:
  - `fontSize`: Pixel font size (default: 18px).
  - `background` & `bgOpacity`: Dark container background with adjustable opacity (0.0 to 1.0).
  - `itemBackground`: Individual bordered background box for each chat message item.
  - `boldUsernames` & `boldMessages`: Bold font weight toggles.
  - `orientation`: `vertical` (sidebar) or `horizontal` (ticker/bottom bar).
  - `platformDisplay`: `icon` (Twitch/Kick platform logos) or `name` (text label).
  - `timestamp`: Shows timestamp (`HH:MM`) on each message.
  - `keepMessages`: Toggle 30s auto-fade vs permanent on-screen message history.
  - `mock`: Realistic simulated stream chat in preview when channels are blank.
- **URL Query Parameters**:
  - `twitch` (string), `kick` (string), `platforms` (`both` | `twitch` | `kick`)
  - `sevenTv` (`true` | `false`), `badges` (`true` | `false`)
  - `fontSize` (number), `font` (`inter` | `roboto` | `nunito` | `mono` | `serif` | `system`)
  - `layout` (`inline` | `stacked` | `card` | `compact`)
  - `animation` (`slide` | `pop` | `bounce` | `stagger` | `fade` | `none`)
  - `background` (`true` | `false`), `bgOpacity` (string: 0.0 - 1.0)
  - `itemBackground` (`true` | `false`)
  - `boldUsernames` (`true` | `false`), `boldMessages` (`true` | `false`)
  - `orientation` (`vertical` | `horizontal`), `platformDisplay` (`name` | `icon`)
  - `timestamp` (`true` | `false`), `keep` (`true` | `false`), `mock` (`true` | `false`)

---

### 3. Raffle Picker & Giveaway Tool (`/setup/raffle`)

A chat-based giveaway and raffle picker tool with an interactive winner celebration overlay for OBS.

- **Chat Keyword Monitoring**: Monitors Twitch IRC or Kick Pusher chat for giveaway entries (default: `!join`, configurable).
- **Anti-Rigging Minimum Duration Timer**: Enforces a minimum duration lock (0 to 300 seconds) before `Draw Winner` unlocks, ensuring all viewers have fair time to enter.
- **Participant Eligibility Rules**:
  - `subscribersOnly`: Restricts giveaway entries exclusively to active channel subscribers.
  - `minSubMonths`: Requires a minimum subscriber loyalty month count to qualify.
  - `maxWinsPerUser`: Limits maximum allowed wins per user (1–5 wins or unlimited).
- **Live Winner Celebration Overlay (`/widgets/raffle-overlay`)**:
  - Broadcasts drawn winner data via browser `BroadcastChannel` (`senchabot-raffle-broadcast`).
  - Triggers a full-screen `canvas-confetti` celebration overlay with customizable winner banner in OBS.
- **Browser Persistence**: All participant entries, drawn winners, and configurations persist in `localStorage` so refreshing or navigating won't erase the giveaway state.

---

### 4. OBS Bridge & Chat OBS Control (`/setup/obs-bridge` & `/tools/obs-bridge`)

A local WebSocket bridge that allows streamers and authorized channel moderators to control OBS Studio scenes and recording directly from chat commands.

- **Local WebSocket Connection**: Connects directly to OBS Studio via `obs-websocket-js` (`ws://localhost:4455`) with optional password authentication. Zero cloud servers or authentication tokens needed.
- **Dynamic Scene Switching (`!scene <name>`)**:
  - Matches chat command arguments against live OBS scenes using case-insensitive exact and substring matching (e.g. `!scene Gaming`, `!scene Just Chatting`, `!scene BRB`).
  - Listens to OBS `SceneListChanged` and `Identified` events to keep the scene registry fresh in real time.
- **Customizable Command Triggers**: Streamers can configure custom trigger words for every action:
  - `cmdScene`: Switch to matching scene (default: `!scene`, can be `scene`, `!switch`, `!goto`).
  - `cmdBrb`: Switch to BRB scene (default: `brb`, can be `afk`, `be-right-back`).
  - `cmdBack`: Switch to Main scene (default: `back`, can be `main`, `imback`).
  - `cmdStartStream` & `cmdStopStream`: Stream management (default: `!startstream` / `!stopstream`, can be `start` / `stop`).
  - `cmdStartRecord` & `cmdStopRecord`: Recording management (default: `!startrecord` / `!stoprecord`, can be `rec` / `stoprec`).
- **User Whitelist (`commandUser`)**: Comma-separated username whitelist ensuring only authorized moderators or broadcasters can execute OBS commands.
- **Scene Assignment Interface**: Interactive scene selector allowing broadcasters to assign `Main` and `BRB` scenes dynamically from their live OBS scene list.
- **URL Query Parameters**:
  - `twitch` (string), `kick` (string), `commandUser` (comma-separated usernames)
  - `obsWebsocketUrl` (string, default: `ws://localhost:4455`), `obsWebsocketPassword` (string)
  - `mainScene` (string), `brbScene` (string)
  - `cmdScene`, `cmdBrb`, `cmdBack`, `cmdStartStream`, `cmdStopStream`, `cmdStartRecord`, `cmdStopRecord`

---

### 5. Emote Wall — Floating Emote Overlay (`/setup/emote-wall`)

A floating emote overlay that turns emote-only Twitch, Kick, and 7TV chat messages into floating on-screen emotes. Normal text messages are ignored.

- **Anonymous Chat Listening**: Connects to Twitch IRC anonymously (`justinfan`) and Kick Pusher WebSocket without account login. The widget itself lives at `/widgets/emote-wall` and can be used directly with URL query parameters.
- **Emote-Only Detection**: A message triggers a floating on-screen emote only when it contains nothing but emotes — a single emote, repeated emotes, or a mix. Supported sources: Twitch native emotes (IRC `emotes` tag), Kick native emotes (`[emote:id:name]` tokens), 7TV channel emotes, and mixed native + 7TV messages.
- **Animation Modes (`mode`)**:
  - `calm` (default): emotes pop up at random screen positions with a fade-in, drift gently away from the spawn point, then fade out.
  - `chaos`: emotes zip in from random screen borders (left, right, top, bottom), travel linearly across the canvas, and vanish randomly once halfway or when reaching the far side.
  - `bounce`: emotes ricochet around like a screensaver, reflecting off the screen edges with a speed boost on every hit.
- **Direct URL examples**:
  - Twitch only: `https://extensions.senchabot.com/widgets/emote-wall?twitch=senchabot`
  - Kick only: `https://extensions.senchabot.com/widgets/emote-wall?kick=senchabot`
  - Both platforms: `https://extensions.senchabot.com/widgets/emote-wall?twitch=senchabot&kick=senchabot`
  - Chaos mode: `https://extensions.senchabot.com/widgets/emote-wall?twitch=senchabot&mode=chaos`
- **URL Query Parameters**:
  - `twitch` (string, Twitch channel name), `kick` (string, Kick channel name)
  - `sevenTv` (`true` | `false`, default `true`)
  - `mode` (`calm` | `chaos` | `bounce`, default `calm`)
  - `subsOnly` (`true` | `false`, default `false`, only subscriber emotes appear)
  - `subDurationX2` (`true` | `false`, default `false`, subscriber emotes stay 2x longer)
  - `showAllEmotes` (`true` | `false`, default `false`, also show emotes mixed into normal text messages)
  - `hypeMode` (`true` | `false`, default `false`, only show an emote after 2+ distinct users send it within 15s)
  - `spamBlock` (`true` | `false`, default `true`, block rapid same-emote repeats from one user)
  - `size` (number 32–256, default `112`), `duration` (seconds 2–30, default `5`), `max` (1–120 simultaneous emotes, default `25`)
  - `mock` (`true` | `false`, simulated emotes for preview/testing)

---

## Development & Deployment Guidelines

- **Zero-Login Philosophy**: All widgets and tools must operate without requiring user registration, authentication, or third-party OAuth flows.
- **Code Style**: Use Biome for formatting and linting. Do not add ESLint or Prettier.
- **Routing**: File-based routing via TanStack Router. Route files are placed in `src/routes/`. The root layout (`__root.tsx`) uses `shellComponent: RootDocument` for TanStack Start SSR/CSR rendering.
- **Styling**: Tailwind CSS v4 is used with high-contrast dark theme defaults (`bg-zinc-950`, `text-zinc-100`).
- **Commands**:
  ```bash
  npm install       # Install dependencies
  npm run dev       # Start dev server on port 3000
  npm run build     # Build for production
  npm run preview   # Preview production build
  npm run deploy    # Deploy to Cloudflare Workers
  npm run test      # Run Vitest tests
  npm run lint      # Run Biome linter
  npm run format    # Run Biome formatter
  npm run check     # Run Biome check
  ```
